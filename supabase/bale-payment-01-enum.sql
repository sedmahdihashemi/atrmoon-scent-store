-- =====================================================================
-- پرداخت بله — قدم ۱ از ۲: اضافه‌کردن مقدار جدید به enum وضعیت سفارش
-- این فایل را تنها و جدا اجرا کنید (Run کنید)، و صبر کنید نتیجه بیاید،
-- قبل از این‌که فایل bale-payment-02-schema.sql را اجرا کنید.
-- دلیل: پستگرس اجازه نمی‌دهد یک مقدار تازه‌اضافه‌شده به enum را در همان
-- تراکنش/اسکریپتی که آن را اضافه کرده استفاده کنید.
-- =====================================================================

ALTER TYPE public.order_status ADD VALUE IF NOT EXISTS 'pending_payment';
